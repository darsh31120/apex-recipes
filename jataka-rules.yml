rules:
  - id: ban-hardcoded-ids
    severity: CRITICAL
    message: "Custom rule hit: hardcoded Salesforce IDs are forbidden in Apex. Query them dynamically."

  - id: require-parallel-tests
    severity: HIGH
    message: "Custom rule hit: all Apex test classes must use @isTest(isParallel=true)."
